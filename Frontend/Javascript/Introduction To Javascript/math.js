var x = 7;
x++; /* Incremend = + (e.g. 7 + 1) */
x--; /* Decremend = - (e.g. 7 - 1) */

x-=3; /* Decremend by 3. */
x+=7; /* Incremend by 7 */
x*=2;
x/=2;

var y = 70;
x+= y; /* x + y */
x-= y; /* x + y */
x*= y;
x/= y;

/*Question 3:

What does y  equal?

    var x = 3;
    var y = x++;
    y += 1;

    2, 3, 4 or 5 */